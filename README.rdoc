== README

A restaurant app built with ember and rails.

Users are able to:

* Add menu items

* Add menu items to tables

The focus of this practice project was to use ember to create relationships between models. We did this successfully, but the app has little use outside of this functionality.
